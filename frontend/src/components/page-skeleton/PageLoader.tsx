
export const PageLoader = () =>{

  return(
    <div className={'h-screen w-screen flex items-center justify-center bg-slate-200'}>
      <div className={'size-20 z-50 bg-transparent border-8 border-b-transparent border-slate-300  rounded-full animate-spin before:content-[""]'} />
    </div>
  )
}